let tasks = []; //En este array se almacenaran las tareas
let taskCount = 0; //Numero entero para contar tareas

const welcomeMessage = `Bienvenido, has añadido ${taskCount} tareas.`; //creacion y manipulacion de strings

console.log(welcomeMessage);